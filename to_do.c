done : handle the printf problem on format[*index] == 'l' && format[*index + 1] == 'l'

handle the precision when it begins by many 0

in process : REinit values at the end of each function for each type

almost done : need to handle ft_printf("%-6% "); (the last space need to be displayed)

done : need to handle : printf ("this is a %s", "string")

need to handle : ft_printf("%-10s%s%.1s%s%s", "this", "is", "aaaaa", "multi", "string");
